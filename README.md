# queue-tracker

Queue Watch — a line tracker & wait-time predictor.

Built with Vue 3 + TypeScript + Vite. Sessions are persisted in browser localStorage.

## Development

```sh
npm install
npm run dev
```

## Build

```sh
npm run build
```
